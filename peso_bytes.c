#include<stdio.h>
#define SIZE 5
main(){
	int a[SIZE];
	printf("Un elemento del areglo ocupa %d bytes \n", sizeof(a[2]));
	printf ("El arreglo de 5 elementos ocupa %d bytes \n", sizeof(a));

return 0;
}
