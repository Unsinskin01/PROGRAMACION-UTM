#include <stdio.h>

int main() {
    int numero, i;
    float raiz;

   
    printf("Ingrese un número entero positivo: ");
    scanf("%d", &numero);
    
    for(i=0;raiz<numero;i++) {
    	raiz=i*i;
    	
}
i--;

printf("\n La raiz cuadrada de %d es: %d", numero, i);

fflush(stdin);
getchar();

return 0;
}

